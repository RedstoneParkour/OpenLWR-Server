syntax = "proto3";

package dose.proto.main;
import "common.proto";

message UECMessage {
  dose.proto.common.Header header     = 1;

  uint32 seq = 2;   // for out-of-order detection only
  uint32 tick_hint = 3;
  uint32 session_id = 4;

  uint32 event_type = 5;

  bytes payload = 6; 
}